package training.map.application.test;

import java.util.*;

class ComparatorSample {
    public static void main(String[] args) {
        //値にListを入れるタイプ
        //クラス別にデータを作る
        List<AllData> allDataList = new ArrayList<>();
        Map<String,AllData> map1 = new HashMap<>();

        allDataList.addAll(Arrays.asList(
                new AllData("たなか",GenderTest.MEN,100,50,70,80,60)

        ));

    }

}
